import 'package:flutter/material.dart';

class GamesTabWidget extends StatelessWidget {
  const GamesTabWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text("games"),
      ),
    );
  }
}
