import 'package:flutter/material.dart';

class FriendsTabWidget extends StatelessWidget {
  const FriendsTabWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text("friends"),
      ),
    );
  }
}
